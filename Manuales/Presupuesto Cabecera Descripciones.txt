Presupuesto Cabecera
=========== ========
// Presupuesto Cabecera es el Antiguo Arbol Pnia
// Requerimiento necesita cambiar el Arbol Pnia + periodo_id(Anho + mes)

// para poder tener la funcionalidad lista se necesita:
	crear una partida : localhost....web/Presupuesto/partida/index
// en web/Presupuesto/presupuesto-cabecera-full/index hacer click en el 2do símbolo de +, el primero genera versiones, no lo usen
	Alli podrán crear un presupuesto cabecera y los periodos eran generados automáticamente.
	
	Deben generar al menos 3 niveles de jerarquía entre los presupuesto cabecera para poder trabajar con requerimientos(los nombres son referenciales) :
	Plan Raiz
	  Plan Proyectos
	    Plan ejecuciones
Usando con ello las funciones:
	PresupuestoCabecera::getComboBoxItems() -> obtendrán Plan Ejecuciones
	PresupuestoCabecera::getComboBoxPeriodosDisponibles( -presupuesto_cabecera_id- ) : los periodos existentes en ese plan 

	


public function getPresupuestosCabeceraId( $usuario_id, $cuantos_niveles_salta)
	return [presupuesto_cabecera_id]; // cuales tengo acceso
	- Retorna las cabeceras relacionadas con el area a la que pertenece el usuario
	- usando tabla externa que une Áreas y Presupuestos
				
Plan = Cuanto manualmente le he puesto : Esto no se toca
// Planificacion = [Raiz : , Ramas: , Hojas:] Esto menos
// desde aqui pueden modificar
Comprometido : Dinero separado que aún no se ha gastado
Devengado	 : (Mateo me dijo q esto era de contaduria) /// paso en contabilidad-Area
Girado		 : Esta por pagar ( aun permanece en las arcas del proyecto) // paso en contabilidad
Pagado		 : Casi el final
Ejecutado	 : Verificación de que fue pagado


Comprometido:
presupuesto_compromiso_ro
presupuesto_compromiso_rooc

Devengado:
presupuesto_devengado_ro
presupuesto_devengado_rooc

Girado:
presupuesto_girado_ro
presupuesto_girado_rooc

Pagado:
presupuesto_pagado_ro
presupuesto_pagado_rooc

Ejecutado:
presupuesto_ejecutado_ro
presupuesto_ejecutado_rooc

NO TOCAR!!!!!!!!!!

presupuesto_ejecucion_posterior_ro
presupuesto_ejecucion_posterior_rooc

presupuesto_saldo_ro
presupuesto_saldo_rooc

presupuesto_saldo_anual_ro
presupuesto_saldo_anual_rooc

!!!!!!!!!!!!!!!!!



/**
     * @param $periodo_id : identificador del periodo almacenado en el requerimiento
     * @param $monto : monto que ha de ser asignado
     * @param $campo : campo al cual se asigna el monto
     * @return bool  : true si se asigno el monto, false, si no se pudo asignar
     */
    public function crearRequerimiento($periodo_id,$monto,$campo)


/**
     * @param $periodo_id : identificador del periodo almacenado en el requerimiento
     * @param $de_campo  : campo del cual se ha de restar el monto a asignar
     * @param $a_campo   : campo al  cual se ha de sumarl el monto a asignar
     * @param $monto     : monto asignado
     * @return bool      : true si el monto se asigno correctametne, false si no se pudo asignar
     */
    public function moverMontos($periodo_id,$de_campo,$a_campo,$monto)



static function PresupuestoCabecera::getComboBoxItemList(){
	/// Solo retornar hojas ( ultimo nivel )
}

/**
     * @param $periodo_id : Periodo en consulta
     * @return ['ro' => float::presupuesto_disponible en Ro, 'Rooc' => float::presupuesto disponible en Rooc]
     */
    public function obtenerPresupuestoDisponible($periodo_id)

/**
     * @param $presupuesto_cabecera_id : identificador de la cabecera almacenada en el requerimiento
     * @return array(periodo_id => anho-mes)
     */
    static function getComboBoxPeriodosDisponibles($presupuesto_cabecera_id)


Para OBTENER UN presupuesto Cabecera:

$objeto_presupuesto_cabecera = PresupuestoCabecera::getPresupuestoCabeceraLink($this->codigo_arbol);

Para ejecutar un requerimiento y Movimiento Montos                       
 $objeto_presupuesto_cabecera->crearRequerimiento($this->periodo_id, $this->monto,'presupuesto_compromiso_ro');
 $objeto_presupuesto_cabecera->moverMontos($this->periodo_id,'presupuesto_compromiso_ro','presupuesto_ejecutado_ro',$this->monto);

